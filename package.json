{
  "name": "contact-us",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:app": "npm run -w @contact-us/app dev",
    "dev:api": "npm run -w @contact-us/api dev",
    "dev": "npm run dev:app & npm run dev:api",
    "build": "echo \"TODO: build by webpack\"",
    "start": "echo \"TODO: start\""
  },
  "dependencies": {},
  "devDependencies": {}
}
